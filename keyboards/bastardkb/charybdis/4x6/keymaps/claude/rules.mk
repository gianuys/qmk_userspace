# OS detection drives OS_MOD/WIN_MOD and the OS_* shortcut keycodes.
OS_DETECTION_ENABLE = yes

# The keymap uses custom keycodes (SAFE_RANGE), which VIA's dynamic keymaps
# cannot represent, so VIA stays disabled.
VIA_ENABLE = no
